module Claw.Control (
    (<=<),
    (&),
    (<&>),
    isPrefixOf,
    isSuffixOf,
    sort,
) where

import Control.Monad ((<=<))
import Data.Function ((&))
import Data.Functor ((<&>))
import Data.List (isPrefixOf, isSuffixOf, sort)
